# IG Authentication

## Requirements
- Use environment variables or a secret store only.
- Never store API keys, passwords, or session tokens in Markdown.
- Support read-only validation and dry-run operation first.

## Open questions
- Confirm exact credential set needed for the target IG account type.
- Confirm account-selection behavior for multiple available accounts.
