# IG Orders

## Purpose
Document dry-run quote generation and eventual live-order submission rules.

## Requirements
- Support quote generation before any live submission.
- Use limit orders where supported by policy.
- Keep explicit approval gates in front of live execution by default.
- Log safe summaries for every order action.
