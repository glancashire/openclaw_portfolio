> **[HISTORICAL]** This plan is superseded. The 204-212 lanes are closed at the gateway-sandbox / cron-hotfix level. For the current backlog see `ROLLUP_OUTSTANDING_PLAN.md`; for the active stabilization sweep see `stabilization-master-plan-2026-05-25.md`.

# Master Plan — Phases 204–212

_Generated 2026-05-22 ~16:15 UTC. Three operator-requested workstreams (A/B/C) plus emergency hot-fixes that surfaced during reconnaissance._

## Reconnaissance findings

### What already exists (don't rebuild)
- **Dashboard**: `runtime/overview/{index,daily-summary,approvals-queue,delivery-status,portfolio-overview,report-history}.html` — generated by `summaryArtifacts.js` + `dashboardGenerator.js`. Has decent badges/cards but no time-series, no FX context, no fill-history graph, no last-N-days mini-charts. Phase 204 was started for richer Markdown rendering of approvals; never committed.
- **Health monitor**: `scripts/run-health-check.js` → `src/reporting/healthReport.js`. Builds JSON+MD+HTML artifacts; supports `--send-email`; uses `effectiveDeliveryPolicy` + `sendEmailMessage` (Mailgun, validated config in `.env`).
- **Cron**: scheduled at 0 8,14,20 UTC daily as `portfolio-health-monitor-etf`. **Currently 100% failing** for 23 consecutive runs.
- **Mailgun**: configured (`MAILGUN_DOMAIN=mailgun.swift.ch`, recipient `lancashire@swift.ch`); `policy.externalDeliveryEnabled=true`; HTML renderer (`emailHtml.js`) in place.
- **Self-heal**: `src/execution/portfolioHealth.js.buildSelfHealPlan()` + `applySafeFixes` flag — exists but limited.

### Critical hot-fixes uncovered (must address)
1. **`consecutiveErrors: 23` on health monitor**, **25 on daily sync**, **11 on rebalance-check**, **2 on weekly report** — *all* failing with: `"Sandbox mode requires Docker, but the Docker daemon is not available."` Docker isn't installed/running on this host and never was; the cron jobs are configured with isolated/sandboxed session targets that demand it.
2. **`delivery.mode: "none"`** on every health/sync cron → even if they ran, Graham gets no notification. Failures are silent.
3. **Telegram broken**: dangling `delivery.mode: "announce"` + missing `chatId` → `"Delivering to Telegram requires target <chatId>"` errors.
4. Stale disabled cron jobs (UBSPX, submit-orders-at-market-open, monitor-trade-fills) — clutter, but not harmful.

### Workspace-wide review (part C)
- **Pre-existing 7 test failures** never addressed: `mailgun CLI arg`, `lifecycle summary deliveryStatus`, `stage-order timing`, `summary-backfill text`, `trade-proposal math`, `trading-guards`, `report-execution-lifecycle-summary`. Flagged across previous phases as out-of-scope.
- **Phase 204 (Markdown annotation rendering)** was implementation-started but never committed. Untracked edit in `src/reporting/summaryArtifacts.js`.
- **No git pre-commit hook / CI** ensuring focused suite runs on PRs.
- **Runtime cleanup**: 27 superseded basket envelopes / 4+ planned reproposals files; mostly handled but worth a periodic sweep.
- **Diagnostic scripts** (probe-*) cluttering `scripts/`; should be moved to `scripts/diagnostics/`.

---

## Phase roadmap (autonomous, no operator gating between phases)

### Phase 204 — Hot-fix: cron sandbox + delivery (BLOCKING)
Cron jobs failing 100% are worse than no cron. Fix first.
- Patch all isolated/sandbox cron jobs to `sandbox: 'off'` or `sessionTarget: 'session:agent:main:main'` (current session, no Docker).
- Patch `delivery.mode: 'none'` → `'announce'` (or webchat-routed) where appropriate.
- Disable the three dead cron jobs (UBSPX-retry, submit-orders-at-market-open, monitor-trade-fills).
- Document fix in TOOLS.md.
- Test: dry-run a single failing job to confirm.

### Phase 205 — Finish Phase 204 from earlier (Markdown annotation rendering)
Commit the unfinished `summaryArtifacts.renderApprovalsQueueMarkdown` work that adds attention + currencyDeployment bullets. Add regression test. (We'd already done the code edit; just need to add a test and commit.)

### Phase 206 — Dashboard v2: stats visualization
Operator request (a). Add to `summaryArtifacts.js` / `dashboardGenerator.js`:
- **Allocation drift bars** — horizontal bar per asset class showing current vs target with color band.
- **Per-instrument health table** with quote-quality tier, last-fill row, % drift.
- **Cron health card** — list jobs + consecutive-errors + last-run age (orange if errors > 0, red if errors > 5).
- **Holdings sparkline data**: snapshot net-liq + cash to `runtime/history/etf/daily-snapshots.jsonl`. Render last-N values as inline SVG sparkline.
- Tests: dashboard renders new sections without breaking existing tests.

### Phase 207 — HTML email digest (periodic)
Operator request (a) tail-end. Daily and weekly HTML email digest piped through `emailHtml.js` → `sendEmailMessage`:
- New CLI: `scripts/send-dashboard-digest.js --portfolio=etf --frequency=daily|weekly`.
- Cron added at 07:00 UTC daily + 17:30 UTC Friday weekly (after SIX close).
- Inline images / SVG charts (no external assets — Mailgun-compatible).
- Tests: snapshot the rendered HTML; assert recipient + subject contracts.

### Phase 208 — Self-healing health monitor v2
Operator request (b). Today's `healthReport.js` has minimal self-heal. Expand:
- **Inbound-request log**: capture last N triggered cron/agent events and their outcomes to `runtime/observability/event-log.jsonl`.
- **Auto-classify** failures by symptom regex (Docker unavailable / 2FA pending / market closed / subscription missing / ECONNREFUSED / etc.) → maps to a `heal` recipe.
- **Heal recipes** (idempotent, conservative):
  - `clear_circuit_breaker_if_subscription_active`,
  - `disable_cron_after_N_consecutive_errors`,
  - `restart_ibkr_gateway_if_socket_dead`,
  - `archive_stale_envelopes`,
  - `sweep_superseded_proposals`.
- **Surface unresolved issues** in the next health email.
- Tests: simulated event-log with known failure patterns → expected heal recipe + open issue surfaced.

### Phase 209 — Health email v2 (HTML report)
Wire Phase 208 self-heal output into the existing health email. Add:
- "Issues auto-healed in this cycle" section.
- "Open issues for operator" section with concrete commands.
- "Trends" (last 7 cron runs success/fail histogram).
- Tests: HTML renderer output for each section.

### Phase 210 — Pre-existing 7 test failures
The 7 failures lurking on `master`. Fix or document why each is acceptable. Don't ship the platform with red tests.

### Phase 211 — Workspace cleanup + diagnostics organization
- Move `probe-*.js` to `scripts/diagnostics/`.
- Sweep `.superseded/` archives older than 30 days.
- Add `npm run test:focused` script that runs the full focused suite.
- Add `.github/workflows/test.yml` (or a local equivalent) to gate `master`.

### Phase 212 — Documentation refresh
- Update `docs/basket-execution-runbook.md` with circuit breaker + subscription gating.
- Add `docs/dashboard-and-health.md` explaining new dashboard + email digest + heal recipes.
- Update `playbook.md`.

---

## Workflow contract
- **One commit per plan + one commit per implementation per phase.**
- Push at end of each phase.
- Focused suite must pass at every phase boundary.
- Progress summary inline.
- No operator interaction unless safety-critical.
